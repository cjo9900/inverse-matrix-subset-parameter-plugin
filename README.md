inverse-matrix-subset-parameter-plugin
======================================

Adds a parameter option to the Parameterized trigger plugin to allow the inverse of the axis that are triggered, to be passed to other jobs.